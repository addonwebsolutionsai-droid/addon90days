{
  "name": "@addonweb/auth",
  "version": "0.1.0",
  "private": true,
  "description": "Canonical Clerk admin gate + IP rate limit. Synced into each product's app/src/lib/ via scripts/sync-libs.mjs.",
  "main": "./src/admin-guard.ts",
  "types": "./src/admin-guard.ts",
  "addonweb": {
    "sync": {
      "destSubdir": "",
      "files": ["admin-guard.ts", "rate-limit.ts"],
      "products": ["p01", "p02", "p03", "p04", "p05", "p06"]
    }
  }
}
