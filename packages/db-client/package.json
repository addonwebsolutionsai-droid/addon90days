{
  "name": "@addonweb/db-client",
  "version": "0.1.0",
  "private": true,
  "description": "Canonical Supabase client. Synced into each product's app/src/lib/supabase.ts via scripts/sync-libs.mjs. database.types.ts stays per-product (each product owns its schema view).",
  "main": "./src/supabase.ts",
  "types": "./src/supabase.ts",
  "addonweb": {
    "sync": {
      "destSubdir": "",
      "files": ["supabase.ts"],
      "products": ["p01", "p02", "p03", "p04", "p05", "p06"]
    }
  }
}
