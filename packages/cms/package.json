{
  "name": "@addonweb/cms",
  "version": "0.1.0",
  "private": true,
  "description": "Canonical CMS database helpers (categories, posts, FAQs, search). Synced into each product's app/src/lib/cms/ via scripts/sync-libs.mjs.",
  "main": "./src/db.ts",
  "types": "./src/db.ts",
  "addonweb": {
    "sync": {
      "destSubdir": "cms",
      "files": ["db.ts"],
      "products": ["p01", "p02", "p03", "p04", "p05", "p06"]
    }
  }
}
