import { PlaceholderPanel } from "@/components/admin/PlaceholderPanel";

export const metadata = { title: "Users" };

export default function Page() {
  return (
    <PlaceholderPanel
      title="Users"
      description="Coming soon - backend ready, dashboard not yet built."
    />
  );
}
