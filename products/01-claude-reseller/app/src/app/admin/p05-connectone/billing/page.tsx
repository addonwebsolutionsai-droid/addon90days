import { PlaceholderPanel } from "@/components/admin/PlaceholderPanel";

export const metadata = { title: "Billing" };

export default function Page() {
  return (
    <PlaceholderPanel
      title="Billing"
      description="Billing layer in progress - invoices and refunds coming soon."
    />
  );
}
