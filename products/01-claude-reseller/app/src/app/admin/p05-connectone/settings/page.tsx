import { PlaceholderPanel } from "@/components/admin/PlaceholderPanel";

export const metadata = { title: "Settings" };

export default function Page() {
  return (
    <PlaceholderPanel
      title="Settings"
      description="Product configuration and integrations coming soon."
    />
  );
}
